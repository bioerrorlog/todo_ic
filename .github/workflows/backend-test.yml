# Run backend test.

name: backend-test
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  backend-test:
    runs-on: ubuntu-latest
    env:
      # MOC_VERSION: 0.6.18
      VESSEL_VERSION: 0.6.2
      DFX_VERSION: 0.8.4
      WASMTIME_VERSION: 0.33.0
    steps:
      - uses: actions/checkout@v2
      - name: install dfx/moc/vessel
        uses:  aviate-labs/setup-dfx@v0.2.3
        with:
          dfx-version: ${{ env.DFX_VERSION }}
          install-moc: true
          vessel-version: ${{ env.VESSEL_VERSION }}
      - name: install wasmtime
        uses: mwilliamson/setup-wasmtime-action@v1
        with:
          wasmtime-version: ${{ env.WASMTIME_VERSION }}
      - name: show versions
        run: |
          dfx --version
          moc --version
          vessel --version
          wasmtime -V
      - name: motoko type checking
        run: |
          for i in src/todo_ic/src/*.mo ; do $(vessel bin)/moc $(vessel sources) --check $i ; done
      - name: module test
        run: |
          make module_test
      - name: canister test
        run: |
          dfx start --background
          dfx deploy --no-wallet todo_ic
          make canister_test
          dfx stop
